{
  "name": "openai-claude",
  "version": "1.0.1",
  "description": "Anthropic-compatible proxy for Azure OpenAI so Claude Code can use GPT deployments",
  "main": "dist/server.js",
  "bin": {
    "openai-claude": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/cli.js",
    "dev": "ts-node src/cli.ts",
    "prepack": "npm run build"
  },
  "keywords": [
    "azure",
    "anthropic",
    "claude",
    "proxy"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "eventsource-parser": "^3.0.6",
    "express": "^5.1.0",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.4",
    "@types/node": "^24.9.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
